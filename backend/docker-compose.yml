version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8010"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/.venv
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8010
    deploy:
      replicas: 2
