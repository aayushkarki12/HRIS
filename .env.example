# Used by docker-compose.yml for the Postgres container.
# Copy to .env at the repo root and fill in a real password. Never commit .env.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=hris_db
