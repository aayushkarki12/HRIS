version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: hris-frontend
    ports:
      - "4002:4002"
    environment:
      VITE_API_URL: ${VITE_API_URL}
